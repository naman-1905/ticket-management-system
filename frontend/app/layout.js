import './globals.css';
export const metadata = { title: 'Beacon · Support tickets', description: 'Support ticket management' };
export default function RootLayout({ children }) { return <html lang="en"><body>{children}</body></html> }
